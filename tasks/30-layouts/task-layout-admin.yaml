id: layout-admin
title: Implement admin layout with outlet
outcome: Layout admin renders all declared slots with correct ACL and outlet content.
layers: ["interface"]
tags: ["layout", "frontend"]
spec_refs: ["spec:layouts.admin"]
checklist:
  - Outlet renders child content correctly
  - Header and nav slots render correctly
  - ACL restricts to authenticated users
  - No server actions needed
