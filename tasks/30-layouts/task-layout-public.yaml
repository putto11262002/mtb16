id: layout-public
title: Implement public layout with outlet
outcome: Layout public renders all declared slots with correct ACL and outlet content.
layers: ["interface"]
tags: ["layout", "frontend"]
spec_refs: ["spec:layouts.public"]
needs: ["action-site-settings-get_settings"]
checklist:
  - Outlet renders child content correctly
  - Banner, header, nav, footer slots render with data from q_settings
  - ACL restricts to public visibility
  - Server action for site-settings is called correctly
