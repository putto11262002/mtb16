id: "cm-schema"
title: "Map spec content model to Drizzle (schema + migrations)"
layers: ["application"]
tags: ["db-schema", "migration"]
spec_refs: ["spec:content_model.types"]
outcome: "Spec content model is represented in Drizzle with migrations applied."
done_checklist:
  - Schema file updated with content model tables
  - Relations defined correctly
  - Migrations generated and applied
  - Constraints match spec requirements
  - Types exported properly
